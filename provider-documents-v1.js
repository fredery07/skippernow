(function(){
"use strict";
const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
const uid=()=>window.currentUser?.id||(typeof currentUser!=="undefined"&&currentUser?.id)||null;
const profile=()=>window.currentProfile||(typeof currentProfile!=="undefined"?currentProfile:null);
const allowed=new Set(["application/pdf","image/jpeg","image/png","image/webp","image/heic","image/heif"]);
function modalShell(title,body){const m=document.createElement("div");m.className="modal show";m.style.zIndex="3000";m.innerHTML=`<div class="dialog" style="max-width:720px"><div class="dialog-top"><div><small>SKIPPERNOW</small><h2>${esc(title)}</h2></div><button class="close" type="button">×</button></div>${body}</div>`;document.body.appendChild(m);m.querySelector(".close").onclick=()=>m.remove();m.addEventListener("click",e=>{if(e.target===m)m.remove();});return m;}
async function listMine(){const id=uid();if(!id)return[];const{data,error}=await db.from("provider_documents").select("id,document_type,label,file_name,mime_type,size_bytes,status,admin_note,created_at,object_path").eq("provider_id",id).order("created_at",{ascending:false});if(error)throw error;return data||[];}
function statusLabel(s){return s==="approved"?"Validé":s==="rejected"?"Refusé":"En attente";}
function statusStyle(s){return s==="approved"?"background:#dcf8ef;color:#057554":s==="rejected"?"background:#ffe6e6;color:#a22e2e":"background:#fff0cf;color:#8a5a00";}
async function openFile(path){const{data,error}=await db.storage.from("provider-documents").createSignedUrl(path,120);if(error)throw error;window.open(data.signedUrl,"_blank","noopener");}
async function openManage(){const m=modalShell("Mes documents",`<p class="muted">Retrouvez les documents envoyés à l'équipe SkipperNow et leur statut de validation.</p><div id="snMyDocs"><p class="muted">Chargement…</p></div>`);const host=m.querySelector("#snMyDocs");try{const docs=await listMine();host.innerHTML=docs.length?docs.map(d=>`<div class="request-card" style="margin-bottom:10px"><div style="display:flex;justify-content:space-between;gap:12px;align-items:flex-start;flex-wrap:wrap"><div><strong>${esc(d.label||d.file_name)}</strong><div class="muted" style="font-size:12px">${esc(d.document_type)} · ${new Date(d.created_at).toLocaleDateString()}</div>${d.admin_note?`<div class="note-box" style="margin-top:8px">${esc(d.admin_note)}</div>`:""}</div><span class="tag" style="${statusStyle(d.status)}">${statusLabel(d.status)}</span></div><div style="margin-top:10px"><button class="small-btn" data-open-doc="${esc(d.object_path)}">Voir le document</button></div></div>`).join(""):`<div class="empty-note">Aucun document envoyé pour le moment.</div>`;host.querySelectorAll("[data-open-doc]").forEach(b=>b.onclick=async()=>{try{await openFile(b.dataset.openDoc);}catch(e){alert(e.message||e);}});}catch(e){host.innerHTML=`<div class="empty-note">${esc(e.message||e)}</div>`;}}
async function openUpload(){const m=modalShell("Envoyer un document",`<p class="muted">Envoyez un justificatif directement au service SkipperNow. Formats acceptés : PDF, JPG, PNG, WEBP, HEIC. 10 Mo maximum.</p><div class="field"><label>Type de document</label><select id="snDocType"><option value="identity">Pièce d'identité</option><option value="insurance">Assurance professionnelle</option><option value="siret">SIRET / justificatif d'entreprise</option><option value="diploma">Diplôme / qualification</option><option value="other">Autre document</option></select></div><div class="field" style="margin-top:10px"><label>Nom du document</label><input id="snDocLabel" placeholder="Ex. Attestation d'assurance 2026"></div><div class="field" style="margin-top:10px"><label>Choisir un fichier ou une photo</label><input id="snDocFile" type="file" accept="application/pdf,image/jpeg,image/png,image/webp,image/heic,image/heif" capture="environment"></div><p id="snDocMsg" class="muted"></p><button id="snDocSend" class="primary wide" type="button">Envoyer à SkipperNow</button>`);const send=m.querySelector("#snDocSend"),msg=m.querySelector("#snDocMsg");send.onclick=async()=>{const file=m.querySelector("#snDocFile").files?.[0];const label=m.querySelector("#snDocLabel").value.trim();const type=m.querySelector("#snDocType").value;if(!file){msg.textContent="Choisissez un fichier ou prenez une photo.";return;}if(file.size>10*1024*1024){msg.textContent="Le fichier dépasse 10 Mo.";return;}if(!allowed.has(file.type)){msg.textContent="Format non accepté.";return;}send.disabled=true;msg.textContent="Envoi sécurisé en cours…";const id=uid();const ext=(file.name.split(".").pop()||"bin").replace(/[^a-z0-9]/gi,"").toLowerCase();const path=`${id}/${Date.now()}-${crypto.randomUUID()}.${ext}`;try{const up=await db.storage.from("provider-documents").upload(path,file,{contentType:file.type,upsert:false});if(up.error)throw up.error;const ins=await db.from("provider_documents").insert({provider_id:id,document_type:type,label:label||file.name,file_name:file.name,object_path:path,mime_type:file.type,size_bytes:file.size});if(ins.error){await db.storage.from("provider-documents").remove([path]);throw ins.error;}msg.style.color="#057554";msg.textContent="Document envoyé ✅ L'équipe SkipperNow le verra dans le tableau de bord admin.";send.textContent="Envoyé";setTimeout(()=>m.remove(),1200);}catch(e){msg.style.color="#a22e2e";msg.textContent=e.message||String(e);send.disabled=false;}};}
function interceptProviderButtons(){document.addEventListener("click",e=>{const b=e.target.closest&&e.target.closest("button");if(!b)return;const txt=(b.textContent||"").trim().toLowerCase();if(txt.includes("envoyer un document")||txt.includes("transmettre un document")){e.preventDefault();e.stopImmediatePropagation();openUpload();}else if(txt.includes("gérer mes documents")||txt==="mes documents"){e.preventDefault();e.stopImmediatePropagation();openManage();}},true);}
async function renderAdmin(){const body=document.querySelector("#dashboardBody");if(!body)return;body.innerHTML=`<div class="panel-head"><div><h3>Documents prestataires</h3><p class="muted">Documents envoyés par les prestataires pour contrôle.</p></div><select id="snAdminDocFilter" style="padding:9px;border:1px solid var(--line);border-radius:10px"><option value="pending">En attente</option><option value="all">Tous</option><option value="approved">Validés</option><option value="rejected">Refusés</option></select></div><div id="snAdminDocs"><p class="muted">Chargement…</p></div>`;const filter=body.querySelector("#snAdminDocFilter"),host=body.querySelector("#snAdminDocs");async function load(){let q=db.from("provider_documents").select("id,provider_id,document_type,label,file_name,object_path,mime_type,size_bytes,status,admin_note,created_at,profiles!provider_documents_provider_id_fkey(full_name,email,provider_activity)").order("created_at",{ascending:false});if(filter.value!=="all")q=q.eq("status",filter.value);const{data,error}=await q;if(error){host.innerHTML=`<div class="empty-note">${esc(error.message)}</div>`;return;}const rows=data||[];host.innerHTML=rows.length?rows.map(d=>{const p=d.profiles||{};return `<article class="request-card" style="margin-bottom:10px"><div style="display:flex;justify-content:space-between;gap:12px;flex-wrap:wrap"><div><strong>${esc(p.full_name||"Prestataire")}</strong><div class="muted">${esc(p.email||"")} · ${esc(p.provider_activity||"")}</div><div style="margin-top:8px"><strong>${esc(d.label||d.file_name)}</strong><div class="muted" style="font-size:12px">${esc(d.document_type)} · ${new Date(d.created_at).toLocaleString()}</div></div></div><span class="tag" style="${statusStyle(d.status)}">${statusLabel(d.status)}</span></div><div style="display:flex;gap:8px;flex-wrap:wrap;margin-top:12px"><button class="small-btn" data-admin-view="${esc(d.object_path)}">Voir</button>${d.status!=="approved"?`<button class="small-btn fill" data-admin-approve="${d.id}">Valider</button>`:""}${d.status!=="rejected"?`<button class="small-btn danger" data-admin-reject="${d.id}">Refuser</button>`:""}</div>${d.admin_note?`<div class="note-box" style="margin-top:10px">Note admin : ${esc(d.admin_note)}</div>`:""}</article>`}).join(""):`<div class="empty-note">Aucun document dans cette catégorie.</div>`;host.querySelectorAll("[data-admin-view]").forEach(b=>b.onclick=async()=>{try{await openFile(b.dataset.adminView);}catch(e){alert(e.message||e);}});host.querySelectorAll("[data-admin-approve]").forEach(b=>b.onclick=()=>review(b.dataset.adminApprove,"approved"));host.querySelectorAll("[data-admin-reject]").forEach(b=>b.onclick=()=>review(b.dataset.adminReject,"rejected"));}
async function review(id,status){const note=prompt(status==="approved"?"Note interne (optionnelle)":"Motif du refus à transmettre au prestataire","")||"";const{error}=await db.from("provider_documents").update({status,admin_note:note||null,reviewed_at:new Date().toISOString(),reviewed_by:uid()}).eq("id",id);if(error){alert(error.message);return;}await load();}filter.onchange=load;load();}
async function installAdminEntry(){const p=profile();if(!p||p.role!=="admin")return;const side=document.querySelector("#dashboardModal .dash-side");if(!side||side.querySelector("[data-panel='provider-documents-admin']"))return;const {count}=await db.from("provider_documents").select("id",{count:"exact",head:true}).eq("status","pending");const b=document.createElement("button");b.type="button";b.dataset.panel="provider-documents-admin";b.innerHTML=`📄 <span>Documents prestataires${count?` (${count})`:""}</span>`;b.onclick=()=>{side.querySelectorAll("button").forEach(x=>x.classList.toggle("active",x===b));renderAdmin();};side.appendChild(b);}
interceptProviderButtons();const obs=new MutationObserver(()=>setTimeout(installAdminEntry,0));obs.observe(document.documentElement,{childList:true,subtree:true});setTimeout(installAdminEntry,800);
window.skProviderDocuments={openUpload,openManage,renderAdmin};
})();
(function(){
  "use strict";

  let selectedBoatId=null;
  let connectScriptPromise=null;
  const moneyRental=cents=>new Intl.NumberFormat(document.documentElement.lang||"fr",{style:"currency",currency:"EUR"}).format(Number(cents||0)/100);
  const escRental=value=>String(value??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  function rentalLabel(status){const fr={pending:"En attente du propriétaire",accepted:"Acceptée — paiement à effectuer",declined:"Refusée",cancelled:"Annulée",confirmed:"Confirmée",in_progress:"En cours",completed:"Terminée"};return fr[status]||status;}
  async function apiCall(fn,body){const{data:{session}}=await db.auth.getSession();if(!session)throw new Error("Reconnectez-vous pour continuer.");const res=await fetch(SUPABASE_URL+"/functions/v1/"+fn,{method:"POST",headers:{"Content-Type":"application/json",Authorization:"Bearer "+session.access_token,apikey:SUPABASE_KEY},body:JSON.stringify(body)});const json=await res.json();if(!res.ok||json.error)throw new Error(json.error||"Opération indisponible");return json;}

  async function payRental(requestId){
    const stripe=window.Stripe?window.Stripe(STRIPE_PUBLISHABLE_KEY):null;if(!stripe){alert("Paiement indisponible.");return;}
    const modal=document.createElement("div");modal.className="modal show";modal.style.zIndex="2500";modal.innerHTML=`<div class="dialog"><div class="dialog-top"><div><small>PAIEMENT SÉCURISÉ</small><h2>Payer la location</h2></div><button class="close" type="button">×</button></div><div class="note-box" style="margin-bottom:12px">Le montant affiché est le <strong>prix final tout compris</strong>.</div><div id="snRentalPaymentElement"></div><p class="error-text" id="snRentalPaymentMsg"></p><button class="primary wide" id="snRentalPayBtn" type="button" style="margin-top:14px;display:none">Payer</button></div>`;document.body.appendChild(modal);modal.querySelector(".close").onclick=()=>modal.remove();const msg=modal.querySelector("#snRentalPaymentMsg");
    try{const init=await apiCall("boat-rental-payment",{action:"create",requestId});if(init.paid){modal.remove();await renderRentalPanel();return;}const elements=stripe.elements({clientSecret:init.client_secret});elements.create("payment").mount("#snRentalPaymentElement");const btn=modal.querySelector("#snRentalPayBtn");btn.style.display="block";btn.textContent="Payer "+moneyRental(init.amount);btn.onclick=async()=>{btn.disabled=true;msg.textContent="";try{const{returnUrl}= {returnUrl:location.origin+"/?boat_rental="+encodeURIComponent(requestId)};const{error,paymentIntent}=await stripe.confirmPayment({elements,confirmParams:{return_url:returnUrl},redirect:"if_required"});if(error)throw error;const done=await apiCall("boat-rental-payment",{action:"confirm",requestId,paymentIntentId:paymentIntent?.id});if(!done.ok)throw new Error("Paiement en cours de confirmation.");modal.remove();await renderRentalPanel();}catch(e){msg.textContent=e.message||String(e);}finally{btn.disabled=false;}};}catch(e){msg.textContent=e.message||String(e);}
  }

  function loadConnectScript(){if(window.StripeConnect?.init)return Promise.resolve(window.StripeConnect);if(connectScriptPromise)return connectScriptPromise;connectScriptPromise=new Promise((resolve,reject)=>{const s=document.createElement("script");s.src="https://connect-js.stripe.com/v1.0/connect.js";s.async=true;s.onload=()=>window.StripeConnect?.init?resolve(window.StripeConnect):reject(new Error("Stripe Connect indisponible"));s.onerror=()=>reject(new Error("Impossible de charger Stripe Connect"));document.head.appendChild(s);});return connectScriptPromise;}
  async function openOwnerPayoutSetup(){
    const dialog=document.createElement("dialog");dialog.style.cssText="border:1px solid #dce8e9;border-radius:20px;padding:24px;width:min(620px,94vw);max-height:90vh;overflow:auto;color:#102b3f";dialog.innerHTML=`<button type="button" style="float:right">×</button><h2>Versements de mes locations</h2><p>Le prix que vous renseignez sur votre bateau correspond au <strong>montant net que vous souhaitez recevoir</strong>. SkipperNow ajoute ses frais au prix présenté au client.</p><p role="status">Vérification…</p><label>Pays <select style="display:block;width:100%;padding:10px;margin:8px 0 14px"><option value="FR">France</option><option value="MC">Monaco</option><option value="ES">Espagne</option><option value="IT">Italie</option></select></label><button class="primary" type="button" data-open>Configurer mes versements</button><div data-connect style="margin-top:18px"></div>`;document.body.append(dialog);dialog.querySelector("button").onclick=()=>{dialog.close();dialog.remove();};dialog.showModal();const status=dialog.querySelector('[role="status"]'),open=dialog.querySelector('[data-open]'),select=dialog.querySelector("select"),mount=dialog.querySelector('[data-connect]');
    try{const st=await apiCall("boat-rental-owner-flow",{action:"account_status"});status.textContent=st.ready?"Coordonnées vérifiées : vous pouvez recevoir les versements.":st.connected?"Compte Stripe créé. Complétez les informations demandées.":"Aucun compte de versement configuré.";if(st.country){select.value=st.country;select.disabled=true;}}catch(e){status.textContent=e.message||String(e);}
    open.onclick=async()=>{open.disabled=true;status.textContent="Ouverture du formulaire sécurisé…";try{const sdk=await loadConnectScript();const fetchClientSecret=async()=>{const r=await apiCall("boat-rental-owner-flow",{action:"embedded_session",country:select.value});return r.client_secret;};const instance=sdk.init({publishableKey:STRIPE_PUBLISHABLE_KEY,locale:document.documentElement.lang||"fr",fetchClientSecret});const st=await apiCall("boat-rental-owner-flow",{action:"account_status"}).catch(()=>({ready:false}));const component=instance.create(st.ready?"account-management":"account-onboarding");mount.replaceChildren(component);status.textContent="";}catch(e){status.textContent=e.message||String(e);open.disabled=false;}};
  }

  async function completeRental(requestId){
    if(!confirm("Confirmer que la location a bien commencé/été réalisée et autoriser les versements aux professionnels ?"))return;
    try{
      const{data:rental}=await db.from("boat_rental_requests").select("skipper_net_cents").eq("id",requestId).single();
      const owner=await apiCall("boat-rental-owner-flow",{action:"complete",requestId});
      let skipper=null;if(rental?.skipper_net_cents){try{skipper=await apiCall("boat-rental-skipper-payout",{requestId});}catch(e){skipper={error:e.message||String(e)};}}
      const messages=[];messages.push(owner.transferred?"Versement propriétaire créé.":(owner.message||"Versement propriétaire en attente."));if(rental?.skipper_net_cents)messages.push(skipper?.transferred?"Versement skipper créé.":"Versement skipper en attente de son compte Stripe.");alert("Location validée. "+messages.join(" "));await renderRentalPanel();
    }catch(e){alert(e.message||String(e));}
  }

  async function renderRentalPanel(){
    const body=document.querySelector("#dashboardBody");if(!body||!currentUser)return;
    const [{data,error},{data:myBoats}]=await Promise.all([db.from("boat_rental_requests").select("id,boat_id,renter_id,owner_id,starts_at,ends_at,duration_type,guest_count,wants_skipper,notes,amount_cents,platform_fee_cents,owner_net_cents,owner_platform_fee_cents,skipper_net_cents,skipper_platform_fee_cents,status,payment_status,created_at,boats(name,brand,model,home_port,photo_url)").order("created_at",{ascending:false}),db.from("boats").select("id").eq("client_id",currentUser.id).limit(1)]);
    if(error){body.innerHTML=`<div class="empty-note">${escRental(friendlyError(error))}</div>`;return;}const rows=data||[];const ownsBoat=(myBoats||[]).length>0;
    body.innerHTML=`<div class="panel-head"><div><h3>Locations de bateaux</h3><p class="muted">Le client voit toujours le prix final. Chaque professionnel voit uniquement ce qu'il recevra.</p></div>${ownsBoat?`<button class="small-btn fill" type="button" data-owner-payout>Configurer mes versements</button>`:""}</div><div class="request-list">${rows.length?rows.map(r=>{const b=r.boats||{};const isOwner=String(r.owner_id)===String(currentUser.id);const title=[b.brand,b.model].filter(Boolean).join(" ")||b.name||"Bateau";const date=new Date(r.starts_at).toLocaleDateString(document.documentElement.lang||"fr-FR");let actions="";if(isOwner&&r.status==="pending")actions=`<button class="small-btn fill" data-rental-action="accept" data-id="${r.id}">Accepter</button> <button class="small-btn" data-rental-action="decline" data-id="${r.id}">Refuser</button>`;if(!isOwner&&["pending","accepted"].includes(r.status)&&r.payment_status==="unpaid")actions+=` <button class="small-btn" data-rental-action="cancel" data-id="${r.id}">Annuler</button>`;if(!isOwner&&r.status==="accepted"&&["unpaid","processing"].includes(r.payment_status)&&(!r.wants_skipper||r.skipper_net_cents))actions+=` <button class="small-btn fill" data-rental-pay="${r.id}">Payer ${moneyRental(r.amount_cents)}</button>`;if(!isOwner&&r.status==="accepted"&&r.wants_skipper&&!r.skipper_net_cents)actions+=` <span class="tag">Recherche d'un skipper en cours</span>`;if(!isOwner&&r.status==="confirmed"&&r.payment_status==="paid"&&new Date(r.starts_at)<=new Date())actions+=` <button class="small-btn fill" data-rental-complete="${r.id}">Valider la location</button>`;const ownerNet=Number(r.owner_net_cents||Math.max(0,Number(r.amount_cents||0)-Number(r.platform_fee_cents||0)));return `<article class="request-card"><div style="display:flex;gap:12px;align-items:flex-start">${b.photo_url?`<img src="${escRental(b.photo_url)}" alt="" style="width:92px;height:72px;object-fit:cover;border-radius:12px">`:""}<div style="flex:1"><strong>${escRental(title)}</strong><div class="muted">📍 ${escRental(b.home_port||"—")} · ${escRental(date)}</div><div style="margin-top:6px"><span class="tag">${escRental(rentalLabel(r.status))}</span> ${!isOwner?`<strong>Prix final : ${moneyRental(r.amount_cents)}</strong>`:""}${r.wants_skipper?` <span class="tag">Skipper demandé</span>`:""}</div>${isOwner&&r.amount_cents?`<div class="note-box" style="margin-top:7px"><strong>Vous recevrez ${moneyRental(ownerNet)} net</strong></div>`:""}${!isOwner&&r.skipper_net_cents?`<div class="muted" style="margin-top:5px">Bateau + skipper inclus dans le prix final.</div>`:""}${r.notes?`<p class="muted">${escRental(r.notes.replace("[[SKIPPER_REQUESTED]]",""))}</p>`:""}<div style="margin-top:10px">${actions}</div></div></div></article>`;}).join(""):`<div class="empty-note">Aucune demande de location pour le moment.</div>`}</div>`;
    body.querySelector("[data-owner-payout]")?.addEventListener("click",openOwnerPayoutSetup);
    body.querySelectorAll("[data-rental-action]").forEach(btn=>btn.addEventListener("click",async()=>{btn.disabled=true;const action=btn.dataset.rentalAction;const next=action==="accept"?"accepted":action==="decline"?"declined":"cancelled";const{error}=await db.from("boat_rental_requests").update({status:next}).eq("id",btn.dataset.id);if(error)alert(friendlyError(error));await renderRentalPanel();}));
    body.querySelectorAll("[data-rental-pay]").forEach(btn=>btn.addEventListener("click",()=>payRental(btn.dataset.rentalPay)));
    body.querySelectorAll("[data-rental-complete]").forEach(btn=>btn.addEventListener("click",()=>completeRental(btn.dataset.rentalComplete)));
  }

  function installDashboardEntry(){const side=document.querySelector(".dash-side");if(!side||side.querySelector("[data-panel='boat-rentals']"))return;const btn=document.createElement("button");btn.type="button";btn.dataset.panel="boat-rentals";btn.innerHTML="🚤 <span>Locations bateaux</span>";btn.addEventListener("click",async()=>{side.querySelectorAll("button").forEach(b=>b.classList.toggle("active",b===btn));await renderRentalPanel();});side.appendChild(btn);}
  function install(){if(typeof window.openBoatDetail==="function"){const originalBoatDetail=window.openBoatDetail;window.openBoatDetail=function(id){selectedBoatId=String(id);return originalBoatDetail.apply(this,arguments);};}const submit=document.querySelector("#bookingSubmit");if(submit)submit.addEventListener("click",async function(ev){if(!bookingContext||bookingContext.activity!=="boat_rental")return;ev.preventDefault();ev.stopImmediatePropagation();const msg=document.querySelector("#bookingMessage");if(!currentUser){msg.textContent=t("booking.needLogin");return;}if(!selectedBoatId){msg.textContent="Bateau introuvable. Fermez puis rouvrez l’annonce.";return;}if(String(bookingContext.targetId||"")===String(currentUser.id)){msg.textContent=t("boatDetail.ownListing");return;}const date=document.querySelector("#bookingDate").value,duration=document.querySelector("#bookingDuration").value,days=duration==="multi"?Math.max(2,Math.round(Number(document.querySelector("#bookingDays").value||2))):1;if(!date||date<localDateStr()){msg.textContent=t(date?"booking.needFutureDate":"booking.needDate");return;}const start=new Date(date+"T09:00:00"),hours=duration==="half"?4:duration==="day"?8:days*24,end=new Date(start.getTime()+hours*3600000),notes=sanitizeContactInfo(document.querySelector("#bookingDetails").value.trim());msg.textContent=t("booking.sending");submit.disabled=true;const wantsSkipper=!!document.querySelector("#snWantsSkipper")?.checked;const{error}=await db.from("boat_rental_requests").insert({boat_id:selectedBoatId,renter_id:currentUser.id,starts_at:start.toISOString(),ends_at:end.toISOString(),duration_type:duration,wants_skipper:wantsSkipper,notes:notes?.replace("[[SKIPPER_REQUESTED]]","").trim()||null});submit.disabled=false;if(error){msg.style.color="#b42318";msg.textContent=friendlyError(error);return;}if(typeof window.skTrackEvent==="function")window.skTrackEvent("boat_rental_created",{boat_id:selectedBoatId,duration,wants_skipper:wantsSkipper});document.querySelector("#bookingForm").style.display="none";document.querySelector("#bookingSuccess").style.display="block";},true);const originalOpenDashboard=window.openDashboard;if(typeof originalOpenDashboard==="function")window.openDashboard=async function(){const result=await originalOpenDashboard.apply(this,arguments);installDashboardEntry();return result;};setTimeout(installDashboardEntry,500);}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",install,{once:true});else install();
})();
